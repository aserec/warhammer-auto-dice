# Roster JSON fixtures

Full copies of `auto-dice/test-data/example-votann-list.json` and `example-tzeentch-list.json` live here for Vitest and parser golden tests (`bs-forces-json-v1`). Do not hand-truncate: weapons and nested profiles must stay intact.

If CI size ever requires smaller files, add separate `*-smoke.json` fixtures that still include at least one complete unit with weapons, and document them here.
